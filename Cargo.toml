[workspace]
members = ["styx-proto", "styx-keymap", "styx-sender", "styx-receiver"]
resolver = "2"

[workspace.package]
edition = "2024"
license = "GPL-3.0-or-later"
repository = "https://github.com/ghreprimand/styx"

[profile.release]
strip = true
lto = "thin"
